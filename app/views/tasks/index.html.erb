<!-- モーダルのトリガーボタン -->
<h1 class="col-md-11 offset-md-1"><%= current_user.name %> の タスク</h1>
<div class="col-md-2 offset-md-5">
  <%= link_to 'NEW TASK CREATE',
                new_task_path,
                data: { remote: true },
                class: "btn btn-primary",
                id: "modalButton",
                "data-bs-toggle" => "modal",
                "data-bs-target" => "#exampleModal" %>
</div>

<%= render 'modal' %>

<div class="task-column col-md-11 offset-md-1">
  <p class="column-state">State</p>
  <p class="column-limit">Limit</p>
  <p class="column-task">Task</p>
</div>

<div id="tasks">
  <%= render @tasks %>
</div>
